/**
 * Asynchronously loads the bootstrap module.
 * This ensures that any shared dependencies (like react or react-dom) 
 * are correctly resolved and available before bootstrap is executed.
 */

import ('./bootstrap')
